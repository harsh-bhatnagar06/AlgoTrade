// ===== BROKER CONFIGURATION (ANGEL ONE) =====
// For security, the API key is NOT stored in the code.
// Each user must provide their own SmartAPI Key in the settings modal.

const ANGEL_ONE_CONFIG = {
  API_KEY: "", // Loaded from UI at runtime
  CLIENT_CODE: "",
  PASSWORD: "",
  TOTP_SECRET: "",
};
