// ===== BROKER CONFIGURATION (ANGEL ONE) =====
// The API key is intentionally NOT stored here.
// It lives in .env on the server and all broker calls go through /api/broker/*.

const ANGEL_ONE_CONFIG = {
  CLIENT_CODE: "",  // Set at runtime via the Broker Settings modal
  PASSWORD: "",
  TOTP_SECRET: "",
};
